{
  "name": "simplelogin-mailbox-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API gateway for SimpleLogin mailbox management",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.67.0"
  }
}
